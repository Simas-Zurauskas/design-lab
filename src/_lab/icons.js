// LAB CHROME — Lucide icon runtime (workbench infrastructure, not design content).
// Ships the FULL icon set so any <i data-lucide="name" class="h-4 w-4"></i> in
// content just works — no system edit needed to use a new icon. Icons render as
// inline SVGs with stroke: currentColor, so they inherit text color utilities.
import { createIcons, icons } from 'lucide';
createIcons({ icons });
